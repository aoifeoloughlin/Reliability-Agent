# Tests

- Checks logic without the host.

## Files in the directory:
- test_conllectors.py: Mock `/proc` and validates the parsing logic.
- test_detectors.py: Test threshold and trend detection.
- test_remediators.py: Validates the safety logic (dry-run mode).